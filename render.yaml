services:
  - type: web
    name: laravel-app
    env: docker
    plan: free
    buildCommand: docker build -t laravel-app .
    startCommand: docker run -p 8080:80 laravel-app

databases:
  - name: laravel-db
    databaseName: laravel
    user: laravel
    plan: free
